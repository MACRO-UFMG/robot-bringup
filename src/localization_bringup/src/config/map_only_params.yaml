map_server:
  ros__parameters:
    use_sim_time: false
    yaml_filename: "maps/quarter_map.yaml"

lifecycle_manager:
  ros__parameters:
    use_sim_time: false
    autostart: true
    node_names: ["map_server"]  # ← só o map_server, sem amcl